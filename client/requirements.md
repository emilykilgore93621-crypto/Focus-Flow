## Packages
framer-motion | For smooth, calming animations and page transitions
date-fns | For human-readable date formatting
clsx | For conditional class names
tailwind-merge | For merging tailwind classes safely

## Notes
- Using Replit Auth (useAuth hook) for user management
- Dashboard requires "Daily Focus" endpoint interaction
- Goals use a simplified status (boolean isCompleted) but we might visualize them in a kanban-like way (To Do / Done)
- Resources serve as both general tips and interview prep materials
